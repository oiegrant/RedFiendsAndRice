namespace System
{
    public class UpgradeManager
    {
        //provide upgrades based on latest enemy defeated
        //upgrade pools have a key associated
        //on each round when a certain enemy is defeated, RoundManager checks for current keys and sees
        //if any more keys are available - grab them and add to upgrade pool
        
        //Also needs to keep track of upgrades already unlocked so we don't double show
        
        
    }
}