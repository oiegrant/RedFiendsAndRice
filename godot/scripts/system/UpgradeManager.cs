namespace RedFiendsAndRice.System;

// Placeholder class — unchanged from Unity. No `MonoBehaviour` inheritance was
// used in the original, so this is a plain class in Godot too.
//
// TODO original Unity notes:
//   - Provide upgrades based on latest enemy defeated.
//   - Upgrade pools have a key associated.
//   - On each round when an enemy is defeated, RoundManager checks for current
//     keys and pulls any newly-available ones into the upgrade pool.
//   - Track upgrades already unlocked so we don't double-show.
public class UpgradeManager
{
}
